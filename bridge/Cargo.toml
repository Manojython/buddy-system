[package]
name = "bridge"
version = "0.1.0"
edition = "2021"

[lib]
# Output file name becomes _bridge.so / _bridge.pyd — must match module-name in pyproject.toml
name = "_bridge"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.29", features = ["extension-module"] }
